VITE_API_BASE_URL=https://api.example.com/api/v1
VITE_ROUTER_MODE=hash
VITE_DESKTOP=true

# 说明：
# 1) 使用 `npm run build:desktop` 进行桌面构建，Vite 会加载本文件。
# 2) Electron 主进程将从 dist/index.html 加载静态资源。
# 3) 桌面端推荐哈希路由以避免 file:// 深链接与刷新问题。